<% layout("/layouts/boilerplate") %> 
<body>
    <!-- Navbar -->
    

    <!-- Hero Section -->
    <header class="hero">
        <div class="hero-content">
            <h1 class="page-title">Welcome to WonderLust</h1>
            <p>Find your perfect travel destination and accommodation</p>
            <a href="/listings" class="btn btn-primary">Explore Now</a>
        </div>
    </header>

    <!-- Featured Listings -->
    <section class="container featured-listings">
        <h2 class="section-title">Featured Destinations</h2>
        <div class="row">
            <% for(let listing of featuredListings) { %>
                <div class="col-md-4">
                    <div class="card">
                        <img src="<%= listing.image %>" class="card-img-top" alt="<%= listing.title %>">
                      <!--   <div class="card-body">
                            <h5 class="card-title"><%= listing.title %></h5>
                            <p class="card-text">&#2547; <%= listing.price.toLocaleString("en-BD") %> / night</p>
                            <a href="/listings/<%= listing.id %>" class="btn btn-outline-primary">View Details</a>
                        </div> -->
                    </div>
                </div>
            <% } %>
        </div>
    </section>

    <!-- Footer -->
   
</body>
</html>
